#include <stdio.h>
#include <limits.h>
#include <math.h>

int main(void)
{
    // printf("CHAR_MAX : %d\n", CHAR_MAX);
    // printf("CHAR_MIN : %d\n", CHAR_MIN);
    // printf("SHORT_MAX : %d\n", SHRT_MAX);
    // printf("SHORT_MIN : %d\n", SHRT_MIN);
    // printf("INT_MAX : %d\n", INT_MAX);
    // printf("INT_MIN : %d\n", INT_MIN);

    // printf("UNSIGNED_CHAR_MAX : %d\n", UCHAR_MAX);
    // printf("UNSIGNED_CHAR_MIN : %d\n", 0);

    // printf("UNSIGNED_SHORT_MAX : %d\n", USHRT_MAX);
    // printf("UNSIGNED_SHORT_MIN : %d\n", 0);

    // printf("UNSIGNED_INT_MAX : %ld\n", UINT_MAX);
    // printf("UNSIGNED_INT_MIN : %d\n", 0);

    // double calc = (3 * pow(11.3, 2.0) * 11.3 - 7 * pow(11.3, 2.0) + 9);

    // printf("x의 값이 11.3일 때, 다항식의 값은 %f 입니다.", calc);

    // printf("\"이것은 출력제어문자를 이용한 실습예제\"\n");
    // printf("\"출력제어문자는 \'\\\' 문자를 사용\"");

    // int a = 37970;
    // int ten_thousand = a / 10000;
    // a = a - ten_thousand * 10000;
    // int five_thousand = a / 5000;
    // a = a - five_thousand * 5000;
    // int thousand = a / 1000;
    // a = a - thousand * 1000;
    // int hundred = a / 100;
    // a = a - hundred * 100;
    // int ten = a / 10;

    // printf("37970원은\n만원짜리 %d개\n오천원짜리 %d개\n천원짜리 %d개\n백원짜리 %d개\n십원짜리 %d개로 구성됩니다.", ten_thousand, five_thousand, thousand, hundred, ten);

    // int year;
    // year = 2005;
    // int number;
    // printf("%d", number);
    double x = 3234523452343434353;
    printf("%lf", x);
}