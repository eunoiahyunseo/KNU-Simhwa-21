#include <stdio.h>

int gCount = 100;

void increment(int);

int main(void)
{
    // gCount = 40;
    // printf("%d\n", gCount);

    // increment(1);
    // return 0;
}

void increment(int i)
{
    printf("%d\n", gCount);
}